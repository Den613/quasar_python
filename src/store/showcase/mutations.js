export function USER (state, {user_data}) {
    state.user_data = user_data
}

export function TABLE (state, {table}) {
    state.table = table
}

export function GROUPS (state, {groups}) {
    state.groups = groups
}

